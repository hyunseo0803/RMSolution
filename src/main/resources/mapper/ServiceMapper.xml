<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="a.b.c.dao.ServiceDAO">
    <select id="findServiceByUserId" parameterType="String">
        SELECT *
        FROM service
        WHERE mid = #{mid}
    </select>
    <insert id="insertService" parameterType="a.b.c.vo.ServiceVO">
        INSERT INTO service(mid, usercnt, kindof, date, month, company, phone, email)
        VALUES (#{mid}, #{usercnt}, #{kindof}, #{date}, #{month}, #{company}, #{phone}, #{email})
    </insert>
</mapper>